# Browse-card doctor details row

Owner requested three equal columns: price, duration, CMS clinician, with two 1px cream vertical dividers. Only Smile Gallery browse-card facts are modified; expanded cards retain their current layout.

Existing clinician field supplies the third text-only span, with normal case, and existing icon-bearing price/duration spans remain. Grid auto-columns give equal widths to available facts; a missing field produces no invented value or orphan divider. Long text wraps within the column. Divider uses the existing orange-3/cream rule colour.

Local verification executed the current card-generation code against all 30 published CMS cases: three details per row, correct clinician values, and two retained SVG icons; controller syntax passed. No new scripts or requests.

Before and after page-head/footer snapshots are included. Rollback: restore before blocks to page 6aa3efd54f3f705caf98c687, retaining later unrelated edits if any, and publish only staging. No production publication.
