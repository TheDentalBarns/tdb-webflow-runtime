(() => { const navbar = document.querySelector('.navbar10_component'); if (!navbar) return; const root = document.documentElement; const isTransparent = navbar.getAttribute('transparent-nav') === 'true'; const mobileQuery = matchMedia('(max-width:767px)'); const revealDistance = 120; const hideDistance = 140; let viewportWidth = innerWidth; let protectedZone = innerHeight * 0.75; let scrollTop = Math.max(scrollY || root.scrollTop || 0, 0); let lastScrollTop = scrollTop; let upwardDistance = 0; let downwardDistance = 0; let transparentState = 0; let hoverState = 0; let dropdownState = 0; let menuOpenState = 0; let transitionLock = 0; let fixedState = 0; let menuTransitionTimer = null; let appliedTransparentState = null; let animationFrame = 0; let currentTransform = ''; let zHoldTimer = 0; let transparentElements = []; let atTop = scrollTop <= 4; const isMobileTransparent = () => isTransparent && mobileQuery.matches; const isProtectedZone = (value) => isMobileTransparent() && value < protectedZone; const isHidden = () => currentTransform.includes('-100%'); function collectTransparentElements() { transparentElements = [ ...document.querySelectorAll( '#nav-button-link,.navbar10_component,.navbar10_logo-link,.navbar10_link,.menu-icon5', ), ]; } if (isTransparent) collectTransparentElements(); function setTransform(value) { if (value === currentTransform) return; currentTransform = value; navbar.style.transform = value; } function updateTransparentState() { if (!isTransparent) return; const value = transparentState || hoverState || dropdownState || menuOpenState; if (value === appliedTransparentState) return; appliedTransparentState = value; if (!transparentElements.length) collectTransparentElements(); transparentElements.forEach((element) => { element.classList.toggle('is-trans', value); }); } function withoutMotion(callback) { const previousTransition = navbar.style.transition; navbar.style.transition = 'none'; callback(); navbar.offsetHeight; if (previousTransition) navbar.style.transition = previousTransition; else navbar.style.removeProperty('transition'); } function setAbsolute() { if (!isMobileTransparent()) return; withoutMotion(() => { navbar.style.position = 'absolute'; navbar.style.transform = 'translateY(0)'; currentTransform = 'translateY(0)'; }); fixedState = 0; transparentState = 0; } function revealNavbar() { if (!isMobileTransparent()) return setTransform('translateY(0)'); transparentState = 1; updateTransparentState(); withoutMotion(() => { navbar.style.position = 'fixed'; navbar.style.transform = 'translateY(-100%)'; currentTransform = 'translateY(-100%)'; }); fixedState = 1; requestAnimationFrame(() => setTransform('translateY(0)')); } function updateTopState() { if (!isTransparent) return; if (!mobileQuery.matches) atTop = false; else if (scrollTop <= 4) atTop = true; else if (scrollTop > 24) atTop = false; root.classList.toggle('tdb-nav-at-top', !!atTop); } function updateScroll() { scrollTop = Math.max(scrollY || root.scrollTop || 0, 0); const delta = scrollTop - lastScrollTop; const insideProtectedZone = isProtectedZone(scrollTop); updateTopState(); if (scrollTop <= 0) { if (isMobileTransparent()) { navbar.style.position = 'absolute'; fixedState = 0; } transparentState = 0; setTransform('translateY(0)'); upwardDistance = 0; downwardDistance = 0; lastScrollTop = scrollTop; updateTransparentState(); animationFrame = 0; return; } if (isMobileTransparent()) { if (insideProtectedZone) { upwardDistance = 0; downwardDistance = 0; if (fixedState && isHidden()) setAbsolute(); } else if (menuOpenState || transitionLock) { if (fixedState) setTransform('translateY(0)'); else revealNavbar(); upwardDistance = 0; downwardDistance = 0; } else if (delta > 0) { upwardDistance = 0; if (fixedState) { downwardDistance += delta; if (downwardDistance > hideDistance) setTransform('translateY(-100%)'); } else { downwardDistance = 0; } } else if (delta < 0) { downwardDistance = 0; upwardDistance += Math.abs(delta); if (upwardDistance > revealDistance) { if (fixedState) { transparentState = 1; setTransform('translateY(0)'); } else { revealNavbar(); } upwardDistance = 0; } } } else if (menuOpenState || transitionLock) { setTransform('translateY(0)'); upwardDistance = 0; downwardDistance = 0; } else if (delta > 0) { upwardDistance = 0; downwardDistance += delta; if (downwardDistance > hideDistance) setTransform('translateY(-100%)'); } else if (delta < 0) { downwardDistance = 0; upwardDistance += Math.abs(delta); if (scrollTop > 100) transparentState = 1; if (upwardDistance > revealDistance) setTransform('translateY(0)'); } lastScrollTop = scrollTop; updateTransparentState(); animationFrame = 0; } function requestUpdate() { if (animationFrame) return; animationFrame = 1; requestAnimationFrame(updateScroll); } addEventListener('scroll', requestUpdate, { passive: true }); addEventListener( 'resize', () => { if (innerWidth !== viewportWidth) { viewportWidth = innerWidth; protectedZone = innerHeight * 0.75; if (isTransparent) collectTransparentElements(); } requestUpdate(); }, { passive: true }, ); function handleBreakpointChange() { viewportWidth = innerWidth; protectedZone = innerHeight * 0.75; if (isMobileTransparent()) { setAbsolute(); } else { navbar.style.removeProperty('position'); fixedState = 0; } requestUpdate(); } mobileQuery.addEventListener ? mobileQuery.addEventListener('change', handleBreakpointChange) : mobileQuery.addListener(handleBreakpointChange); if (isTransparent) { if (matchMedia('(min-width:768px)').matches) { navbar.addEventListener('mouseenter', () => { hoverState = 1; updateTransparentState(); }); navbar.addEventListener('mouseleave', () => { hoverState = 0; updateTransparentState(); }); } const dropdownToggle = document.querySelector('.navbar10_dropdown-toggle'); if (dropdownToggle) { new MutationObserver(() => { dropdownState = dropdownToggle.classList.contains('w--open'); updateTransparentState(); }).observe(dropdownToggle, { attributes: true, attributeFilter: ['class'], }); } const menuButton = document.querySelector('.navbar10_menu-button'); const menu = document.querySelector('.w-nav-menu'); const overlay = document.querySelector('.w-nav-overlay'); function unlockMenuTransition() { clearTimeout(menuTransitionTimer); menuTransitionTimer = null; transitionLock = 0; navbar.classList.remove('tdb-menu-transitioning'); } function startMenuTransition(event) { if (transitionLock) { event.preventDefault(); event.stopImmediatePropagation(); return; } transitionLock = 1; navbar.classList.add('tdb-menu-transitioning'); setTransform('translateY(0)'); clearTimeout(menuTransitionTimer); menuTransitionTimer = setTimeout(unlockMenuTransition, 620); } if (menuButton) menuButton.addEventListener('click', startMenuTransition, true); function updateMenuState() { menuOpenState = !!( (menuButton && menuButton.classList.contains('w--open')) || (menu && menu.classList.contains('w--open')) || (overlay && overlay.classList.contains('w--open')) ); clearTimeout(zHoldTimer); if (menuOpenState) navbar.classList.add('z-hold'); else zHoldTimer = setTimeout(() => navbar.classList.remove('z-hold'), 450); updateTransparentState(); } const menuObserver = new MutationObserver(updateMenuState); [menuButton, menu, overlay].filter(Boolean).forEach((element) => { menuObserver.observe(element, { attributes: true, attributeFilter: ['class'], }); }); updateMenuState(); updateTransparentState(); } if (isMobileTransparent()) navbar.style.position = 'absolute'; setTransform('translateY(0)'); updateScroll(); })(); (() => { const initMobileNavTextClose = () => { const button = document.querySelector('.navbar10_menu-button, .w-nav-button'); const menu = document.querySelector('.navbar10_menu'); if (!button || !menu || button.dataset.tdbTextClose === '1') return; button.dataset.tdbTextClose = '1'; let textAnimations = []; const menuIsOpen = () => button.classList.contains('w--open') || button.getAttribute('aria-expanded') === 'true' || menu.hasAttribute('data-nav-menu-open'); const clearTextAnimations = () => { textAnimations.forEach((animation) => { try { animation.cancel(); } catch (error) {} }); textAnimations = []; }; const animateTextOut = () => { if (!menuIsOpen()) return; clearTextAnimations(); menu.querySelectorAll('.navbar10_menu-left, .navbar10_menu-right').forEach((element) => { const computed = getComputedStyle(element); const startTransform = computed.transform === 'none' ? 'translate3d(0, 0, 0)' : computed.transform; const startOpacity = Number.parseFloat(computed.opacity); const animation = element.animate( [ { opacity: Number.isFinite(startOpacity) ? startOpacity : 1, transform: startTransform, offset: 0, }, { opacity: 0.5, transform: 'translate3d(0, -0.2rem, 0)', offset: 0.2, }, { opacity: 0.15, transform: 'translate3d(0, -0.45rem, 0)', offset: 0.42, }, { opacity: 0, transform: 'translate3d(0, -0.75rem, 0)', offset: 0.68, }, { opacity: 0, transform: 'translate3d(0, -0.95rem, 0)', offset: 1, }, ], { duration: 420, easing: 'cubic-bezier(0, 0, 0.2, 1)', fill: 'forwards', }, ); textAnimations.push(animation); }); setTimeout(clearTextAnimations, 470); }; button.addEventListener( 'pointerdown', () => { if (button.closest('.navbar10_component')?.classList.contains('tdb-menu-transitioning')) { return; } if (menuIsOpen()) animateTextOut(); else clearTextAnimations(); }, { capture: true, passive: true }, ); button.addEventListener( 'keydown', (event) => { if (event.key !== 'Enter' && event.key !== ' ') return; if (button.closest('.navbar10_component')?.classList.contains('tdb-menu-transitioning')) { return; } if (menuIsOpen()) animateTextOut(); else clearTextAnimations(); }, true, ); }; if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initMobileNavTextClose, { once: true }); } else { initMobileNavTextClose(); } })();
