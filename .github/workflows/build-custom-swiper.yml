name: Build custom Swiper

on:
  push:
    branches:
      - build/custom-swiper-8.4.7
    paths:
      - 'tools/custom-swiper/**'
      - '.github/workflows/build-custom-swiper.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: tools/custom-swiper/package-lock.json

      - name: Install exact build dependencies
        working-directory: tools/custom-swiper
        run: npm install --package-lock-only && npm ci

      - name: Build custom Swiper
        working-directory: tools/custom-swiper
        run: npm run build

      - name: Check artifact
        working-directory: tools/custom-swiper
        run: npm run check

      - name: Commit generated artifact
        run: |
          if git diff --quiet -- dist/tdb-swiper-8.4.7.min.js tools/custom-swiper/package-lock.json; then
            echo 'Generated files are already current.'
            exit 0
          fi

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add dist/tdb-swiper-8.4.7.min.js tools/custom-swiper/package-lock.json
          git commit -m 'Build custom Swiper 8.4.7 artifact'
          git push
